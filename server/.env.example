PORT=4100
JWT_SECRET=replace-with-a-long-random-string

DB_HOST=localhost
DB_PORT=3306
DB_USER=invoice_maker_app
DB_PASSWORD=
DB_NAME=invoice_maker

# Comma-separated list of allowed frontend origins in production, e.g. https://your-app.vercel.app
# Leave unset for local dev (allows all origins).
CORS_ORIGIN=
