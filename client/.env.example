# Leave unset for local dev (Vite's proxy in vite.config.js handles /api locally).
# In production (Vercel), set this to your deployed Railway backend URL, no trailing slash,
# e.g. https://invoice-maker-production.up.railway.app
VITE_API_URL=
